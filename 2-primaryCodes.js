// Our firsts codes in JavaScript learning is alert box and prompt box.

alert("Message that you want send")

// The alert code have a simple syntax. Alert's key word, followed by your message between ().

prompt("Your message about input type information")

// The prompt code serves to open a dialog box to input datas from user. Her is very similar as alert. Both are dialog box.
// Her syntax follow the pattern prompt's key word, followed by your message between (). The diference are that alert merely informs, but the prompt dialoge with user and requere your input information.